# Environment Variables Template
# 
# Spring Boot automatically recognizes these standard environment variable names.
# No need to prefix with SPRING_ - Spring Boot maps them automatically:
#   SPRING_DATASOURCE_URL → spring.datasource.url
#   SPRING_DATASOURCE_USERNAME → spring.datasource.username
#   SPRING_DATASOURCE_PASSWORD → spring.datasource.password
#   SPRING_DATASOURCE_DRIVER_CLASS_NAME → spring.datasource.driver-class-name
#
# For local development:
#   1. Copy this file to .env (not committed to git)
#   2. Fill in your actual values
#   3. Use direnv or export them manually
#
# For production (Render):
#   Set these in Render's environment variables dashboard

# ===========================
# Database Configuration (Development)
# ===========================
# Replace with your actual Neon Development database credentials
SPRING_DATASOURCE_URL=jdbc:postgresql://your-dev-host:5432/neondb?sslmode=require
SPRING_DATASOURCE_USERNAME=your_dev_username
SPRING_DATASOURCE_PASSWORD=your_dev_password
SPRING_DATASOURCE_DRIVER_CLASS_NAME=org.postgresql.Driver

# ===========================
# Database Configuration (Production)
# ===========================
# Use these for production deployment (Render, etc.)
# Replace with your actual Neon Production database credentials
# SPRING_DATASOURCE_URL=jdbc:postgresql://your-prod-host:5432/neondb?sslmode=require
# SPRING_DATASOURCE_USERNAME=your_prod_username
# SPRING_DATASOURCE_PASSWORD=your_prod_password
# SPRING_DATASOURCE_DRIVER_CLASS_NAME=org.postgresql.Driver

# ===========================
# JWT Configuration
# ===========================
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRATION=3600000

# ===========================
# Spring Profile
# ===========================
SPRING_PROFILES_ACTIVE=dev

# ===========================
# Server Port (optional - defaults to 8080)
# ===========================
# PORT=8080

